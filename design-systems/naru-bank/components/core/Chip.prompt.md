Use `Chip` for filter rails (#사회초년생, 자산·건강). 40px tall pills, 8px gap, horizontally scrollable, first item often a 검색 chip.

```jsx
<Chip selected>자산·건강</Chip>
<Chip onClick={()=>setTab('life')}>생활·통신</Chip>
```
