Use `EmptyState` when a list has nothing to show. Explain what will appear here, then offer the action that fills it.

```jsx
<EmptyState title="등록된 계좌가 없어요" description="다른 은행 계좌를 연결하면 한눈에 볼 수 있어요"
  action={<Button variant="outline">계좌 등록하기</Button>}/>
```
