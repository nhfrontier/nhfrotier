Use `SectionHeader` above each block on a scrolling screen. Sits on the grey page background, never inside the card.

```jsx
<SectionHeader title="내 모임" onAction={goGroups}/>
<SectionHeader title="다가오는 금융일정" action="전체보기"/>
```
