Use `Badge` to label state or a headline number inside dense content. Never tappable — reach for `Chip` if it filters.

```jsx
<Badge tone="info">출금예정</Badge>
<Badge tone="success" size="sm">D-12</Badge>
```
