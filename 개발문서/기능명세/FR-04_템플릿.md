# FR-04 Template v1.0

> **관련 문서**: [기능명세 인덱스](개발문서/기능명세/00_INDEX.md) | [PRD](개발문서/01_PRD.md) | [IA·화면](개발문서/03_IA_FUNCTION_SPEC.md) | [API/DB](개발문서/05_API_DB_SPEC.md)

## 1. 개요

NH 공통 Template과 개인/부서 Template을 구분하고 사용 이력과 버전을 관리한다. (PRD 9절)
새 디자인 시스템을 만들지 않고, 기존 NH 자산을 "업무 자산 계층"으로 제공해 NH다운 결과물을 만든다(기획안 1-2절).

## 2. 동작 상세

- Template은 NH 공통 / 개인·부서 소유로 구분된다. IA 기준 진입: Template(D) → NH Template(D1) / My Template(D2).
- Template은 용도에 따라 **두 유형**으로 나뉜다(`template_type`).
  - `DOCUMENT` — 보고서·PPT 등 문서 양식 (기존 개념)
  - `BRAND_ASSET` — **NH 브랜드 자산.** 로고 SVG, 캐릭터, 컬러 토큰, 서체 규칙, 규격 프리셋, 카드 필수 표기 요소 규칙. [FR-13](개발문서/기능명세/FR-13_브랜드시안제작.md)의 시안 생성이 이 자산만 사용한다
- `BRAND_ASSET`은 AI가 만들지 못하는 그림 요소(로고·캐릭터)를 공급하는 유일한 경로다. AI는 등록된 자산을 **배치만** 하고 새로 그리지 않는다.
- ~~저장소의 `design-system/`(토큰·컴포넌트 자산)은 실제 NH 자산 반입 전까지 `BRAND_ASSET`의 원형이자 검증용 대체재로 쓸 수 있다.~~
  → **디자인 시스템을 복수로 보유하고 생성 시점에 고른다.** 저장소의 `design-systems/` 아래 폴더 하나가 디자인 시스템 하나이며, 선택지 정본은 `design-systems/registry.json`이다. 이것이 `BRAND_ASSET`의 현재 실현체다.

| id | 라벨 | 표면 | 성격 |
|---|---|---|---|
| `naru-bank` | 올원뱅크 (모바일 앱) | 모바일 360×780 | **NH 자산이 아니다.** 실체는 창작 브랜드 "나루뱅크"(teal 팔레트, 로고 없음). 실제 NH 자산 반입 전까지 검증용 대체재다 |
| `nh-ibz` | NH 기업인터넷뱅킹 (웹) | 웹 1200px | **컬러 토큰은 실제 NH 값**(NH Blue `#0094D9` · NH Green `#00A04E` · navy `#122F50`). 로고·서체·아이콘은 여전히 대체재다 |

  자산의 실체와 대체재 목록은 [design-systems/README.md](design-systems/README.md).
- 선택한 디자인 시스템은 **화면 계획(1단계)과 화면 생성(2단계) 양쪽의 AI 컨텍스트로 전달된다.** 표면(mobile/web)이 화면 흐름을, 토큰이 색·간격·반경을 결정한다 → [FR-05](개발문서/기능명세/FR-05_AI제작.md)
- 선택값은 **버전마다 기록된다**(`mockup_versions.design_system_id`). 같은 프로젝트에서 v1은 모바일, v2는 웹으로 만들 수 있고 어느 시스템으로 만든 버전인지 화면에 표시된다.
- 요소 단위 AI 편집도 그 버전을 만든 디자인 시스템을 그대로 쓴다. 편집 결과만 다른 팔레트로 나오지 않게 하기 위함이다.
- 디자인 시스템을 추가하려면 `design-systems/<id>/`에 폴더를 넣고 `registry.json`에 한 줄 더한다. 읽는 것은 `<id>/_ds_manifest.json`의 `tokens[]`·`templates[]` 두 개뿐이라 코드를 고칠 일이 없다.
- (❓ NH 실제 브랜드 자산의 반입 경로와 관리 주체 미결 — [결정/미결](개발문서/08_DECISIONS_OPEN_ISSUES.md))
- Template 버전을 선택할 수 있다. 버전은 `template_versions`로 관리한다(TEMPLATE 1:N TEMPLATE_VERSION).
- 사용자가 선택한 Template은 AI 요청 컨텍스트로 전달된다 → [FR-05](개발문서/기능명세/FR-05_AI제작.md)
- Template 사용 이력을 남겨 재사용률(성공 지표)을 측정할 수 있게 한다.
- 후속 고도화: Template 추천, Marketplace(PRD 4절 후속·P5).

## 3. 관련 화면

| 화면 | 역할 |
|---|---|
| S03 Project Workspace | 좌측 Template 선택 |
| S04 AI Chat | Template 선택(생성 컨텍스트 지정) |
| S08 Brand Concept Brief | 브랜드 자산(`BRAND_ASSET`) 선택 — 선택 UI만 제공하며 등록·관리는 다루지 않는다 |
| S10 Design Asset Library | IA `D3`. 보유 자산을 종류별로 펼쳐 보여주고 프로젝트에 담는 진입점. `BRAND_ASSET`의 열람 표면이다 |
| Template (정적 목업 `mockup-site/template.html`) | IA `D`. 디자인 시스템을 고르고 그 시스템의 화면 템플릿·문서 양식을 보여준다 |
| ~~❓ 미정의~~ | ~~IA의 D/D1/D2(Template 관리) 노드에 대응하는 S0x 화면이 정의되지 않음~~ → S10으로 해소. 단 Template의 **버전 선택·소유 구분(D1/D2)** 관리 화면은 여전히 미정의다 |

정본: [03_IA_FUNCTION_SPEC.md](개발문서/03_IA_FUNCTION_SPEC.md)

## 4. 관련 API

| Method | Path | 용도 |
|---|---|---|
| GET | `/api/v1/templates` | 목록 |
| GET | `/api/v1/templates/{templateId}` | 상세 |
| GET | `/api/design-systems` | (프로토타입) 선택 가능한 디자인 시스템 목록. 운영에서는 `/templates?type=BRAND_ASSET`으로 흡수된다 |

정본: [05_API_DB_SPEC.md](개발문서/05_API_DB_SPEC.md)

## 5. US 매핑

| US | 요지 | 핵심 AC 요약 |
|---|---|---|
| US-010 | Template 선택 | NH 공통/개인·부서 구분, 버전 선택 가능, 선택 Template이 AI 컨텍스트로 전달 |
| US-012 | 브랜드 자산 선택 | `BRAND_ASSET` 유형 조회, 선택 자산이 AI 컨텍스트로 전달, 자산 없이도 생성 가능하되 안내 표시 |

AC 전문 정본: [02_USER_STORIES_AC.md](개발문서/02_USER_STORIES_AC.md)

## 6. 구현 상태

| 단계 | 상태 | 근거 |
|---|---|---|
| 설계 | ✅ | PRD FR-04 · US-010 · `/templates` API 반영 |
| 프로토타입 검증 | 🔶 부분 | ~~미검증~~ → ~~`mockup/app/proto/assets/`(S10)에서 자산 **열람**만 검증. Template 선택·버전·`template_type` 구분은 여전히 미검증~~ → **디자인 시스템 선택이 생성까지 이어지는 경로를 검증했다.** `mockup/lib/canvas/designSystem.ts`(레지스트리) · `GET /api/design-systems` · 생성 탭 셀렉트 · `mockup_versions.design_system_id`. Template **버전 선택**과 **소유 구분(D1/D2)**, `DOCUMENT` 유형의 실제 적용은 여전히 미검증 |
| 운영 구현 | ⬜ 미착수 | P1 — 티켓 BE-005 ([백로그](개발문서/06_DEVELOPMENT_BACKLOG.md)) |
